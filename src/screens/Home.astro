---
import BannerSlider from '@/components/Slider/BannerSlider';
import BlogSection from '@/components/ui/BlogSection.astro';
import HomeProductSection from '@/components/ui/HomeProductSection';
---

<Fragment>
  <BannerSlider client:load/>

  <HomeProductSection client:load/>

  <div class='text-center mb-2'>
    <h3 class='text-2xl md:text-4xl font-bold text-[#151875]'>
      Bài viết mới nhất
    </h3>
    <BlogSection />
  </div>

  <style>
    @keyframes bounce {
      0%,
      100% {
        transform: translateY(-50%);
      }
      50% {
        transform: translateY(calc(-50% - 6px));
      }
    }

    .animate-bounce {
      animation: bounce 0.8s infinite;
    }
  </style>
</Fragment>
